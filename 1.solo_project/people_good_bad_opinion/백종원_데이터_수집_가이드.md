# 백종원 vs 임성근 여론 비교 분석 - 실행 가이드

## 📋 사전 준비

### 1. Supabase 테이블 생성
```sql
-- Supabase SQL Editor에서 실행
-- database/baek_jongwon_schema.sql 파일 내용 복사하여 실행
```

### 2. YouTube 영상 URL 수집
`baek_jongwon_link.txt` 파일에 분석할 영상 URL 추가

#### 수집 대상 영상
**논란 시기 (2025년 2월~3월)**
- 백종원 공식 채널 영상
- 빽햄 관련 영상
- 사과/해명 영상
- 뉴스 영상

**현재 시기 (2025년 12월~2026년 1월)**
- 최근 업로드 영상 (회복 상태 측정용)

#### YouTube 검색 키워드
```
1. "백종원 빽햄" + 날짜 필터: 2025.02.01 ~ 2025.03.31
2. "백종원 원산지" + 날짜 필터: 2025.02.01 ~ 2025.03.31
3. "백종원 사과" + 날짜 필터: 2025.02.01 ~ 2025.03.31
4. "백종원" + 날짜 필터: 2025.12.01 ~ 2026.01.31
```

---

## 🚀 실행 순서

### Step 1: 백종원 논란 시기 데이터 수집 (2025년 2-3월)
```bash
cd Opinion_Analysis
python3 1_collect_baek_jongwon.py --period controversy --limit 500
```

**옵션 설명:**
- `--period controversy`: 논란 시기 데이터로 표시
- `--limit 500`: 영상당 최대 500개 댓글 수집
- `--file baek_jongwon_link.txt`: URL 목록 파일 (기본값)

### Step 2: 백종원 현재 시기 데이터 수집 (2025년 12월~2026년 1월)
```bash
python3 1_collect_baek_jongwon.py --period current --limit 500
```

### Step 3: 텍스트 정규화 및 키워드 추출
```bash
python3 2_normalize_baek_jongwon.py
```

**처리 내용:**
- 특수문자 제거
- 불용어 제거 (500개 이상)
- 키워드 추출

### Step 4: BERT 감성 분석
```bash
python3 3_local_analysis_baek_jongwon.py
```

**처리 내용:**
- KoELECTRA 모델 사용
- 긍정/부정/중립 분류
- 신뢰도 점수 산출

### Step 5: DeepSeek LLM 정밀 분석
```bash
python3 4_llm_analysis_baek_jongwon.py
```

**처리 내용:**
- DeepSeek LLM 사용
- **임성근과 동일한 6가지 감정 카테고리 (0-5)**
  - 0: support (응원, 지지, 복귀 기대)
  - 1: anger (분노, 욕설, 강한 적대감)
  - 2: neutral (중립, 무의미한 나열)
  - 3: disappointment (실망, 유감, 팬이었으나 돌아섬)
  - 4: sarcasm (조롱, 비꼼, 반어법)
  - 5: inquiry (질문, 정보 요청, 유저 간 분쟁)

---

## 📊 데이터 확인

### Supabase에서 데이터 확인
```sql
-- 전체 데이터 개수
SELECT COUNT(*) FROM baek_jongwon_youtube_comments;

-- 시기별 데이터 개수
SELECT collection_period, COUNT(*) 
FROM baek_jongwon_youtube_comments 
GROUP BY collection_period;

-- 감정 분석 완료 여부
SELECT 
    COUNT(*) as total,
    COUNT(keywords) as keywords_done,
    COUNT(sentiment_label) as bert_done,
    COUNT(llm_sentiment) as llm_done
FROM baek_jongwon_youtube_comments;

-- 논란 시기 감정 분포 (6가지 카테고리)
SELECT 
    llm_sentiment,
    CASE 
        WHEN llm_sentiment = 0 THEN 'support'
        WHEN llm_sentiment = 1 THEN 'anger'
        WHEN llm_sentiment = 2 THEN 'neutral'
        WHEN llm_sentiment = 3 THEN 'disappointment'
        WHEN llm_sentiment = 4 THEN 'sarcasm'
        WHEN llm_sentiment = 5 THEN 'inquiry'
    END as category,
    COUNT(*) as count
FROM baek_jongwon_youtube_comments 
WHERE collection_period = 'controversy'
GROUP BY llm_sentiment 
ORDER BY llm_sentiment;

-- 현재 시기 감정 분포
SELECT 
    llm_sentiment,
    CASE 
        WHEN llm_sentiment = 0 THEN 'support'
        WHEN llm_sentiment = 1 THEN 'anger'
        WHEN llm_sentiment = 2 THEN 'neutral'
        WHEN llm_sentiment = 3 THEN 'disappointment'
        WHEN llm_sentiment = 4 THEN 'sarcasm'
        WHEN llm_sentiment = 5 THEN 'inquiry'
    END as category,
    COUNT(*) as count
FROM baek_jongwon_youtube_comments 
WHERE collection_period = 'current'
GROUP BY llm_sentiment 
ORDER BY llm_sentiment;

-- 임성근과 비교 (부정적 감정 비율)
SELECT 
    '백종원_논란시' as target,
    ROUND(100.0 * SUM(CASE WHEN llm_sentiment IN (1,3,4) THEN 1 ELSE 0 END) / COUNT(*), 2) as negative_ratio
FROM baek_jongwon_youtube_comments 
WHERE collection_period = 'controversy'
UNION ALL
SELECT 
    '백종원_현재' as target,
    ROUND(100.0 * SUM(CASE WHEN llm_sentiment IN (1,3,4) THEN 1 ELSE 0 END) / COUNT(*), 2) as negative_ratio
FROM baek_jongwon_youtube_comments 
WHERE collection_period = 'current'
UNION ALL
SELECT 
    '임성근_현재' as target,
    ROUND(100.0 * SUM(CASE WHEN llm_sentiment IN (1,3,4) THEN 1 ELSE 0 END) / COUNT(*), 2) as negative_ratio
FROM im_sung_gen_youtube_comments;
```

---

## ⚠️ 주의사항

### 1. API 제한
- YouTube Data API 할당량: 10,000 유닛/일
- 댓글 수집: 영상당 약 1-2 유닛 소모
- 하루에 약 5,000-10,000개 댓글 수집 가능

### 2. DeepSeek API 비용
- 배치당 약 20개 댓글 처리
- 비용 발생 가능 (DeepSeek 요금제 확인 필요)

### 3. 처리 시간
- 1단계 (수집): 영상당 2-5분
- 2단계 (정규화): 50개/배치, 0.3초
- 3단계 (BERT): 100개/배치, 10-15초
- 4단계 (LLM): 20개/배치, 5-10초

---

## 🔧 문제 해결

### 문제: "baek_jongwon_youtube_comments 테이블이 없습니다"
**해결:** Supabase에서 `database/baek_jongwon_schema.sql` 실행

### 문제: "YouTube API 할당량 초과"
**해결:** 
1. 내일까지 대기
2. 추가 API 키 발급
3. `--limit` 값을 줄여서 실행

### 문제: "DeepSeek API 오류"
**해결:**
1. `.env` 파일에 `DEEPSEEK_API_KEY` 확인
2. API 키 유효성 확인
3. 잔액 확인

### 문제: "analyzer 모듈을 찾을 수 없습니다"
**해결:**
```bash
# analyzer 디렉토리 확인
ls -la analyzer/

# deepseek_baek_jongwon_analyzer.py 파일 존재 확인
ls -la analyzer/deepseek_baek_jongwon_analyzer.py
```

---

## 📈 예상 결과

### 수집 목표
- **논란 시기**: 약 5,000개 댓글
- **현재 시기**: 약 2,000개 댓글
- **총 7,000개** 댓글

### 분석 결과 예상 (6가지 카테고리 기준)
```
백종원 논란 시기 (2025.02-03):
- support (0): 5-10%
- anger (1): 40-50%
- neutral (2): 5-10%
- disappointment (3): 20-25%
- sarcasm (4): 15-20%
- inquiry (5): 5-10%
→ 부정적 감정 (1,3,4): 약 80-90%

백종원 현재 (2025.12-2026.01):
- support (0): 30-40%
- anger (1): 15-20%
- neutral (2): 15-20%
- disappointment (3): 10-15%
- sarcasm (4): 10-15%
- inquiry (5): 5-10%
→ 부정적 감정 (1,3,4): 약 35-50%

임성근 현재 (2026.01):
- support (0): 10-15%
- anger (1): 35-45%
- neutral (2): 10-15%
- disappointment (3): 15-20%
- sarcasm (4): 15-20%
- inquiry (5): 5-10%
→ 부정적 감정 (1,3,4): 약 65-85%
```

---

## 🎉 완료 후

모든 단계 완료 후:
1. Supabase에서 데이터 확인
2. 비교 대시보드 구축
3. 회복 곡선 예측 모델 개발
4. 권장 대응 전략 도출

**다음 문서:** `비교_대시보드_구축_가이드.md` (작성 예정)
